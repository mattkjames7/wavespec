name: Release Package

on:
  workflow_dispatch:
    inputs:
      test:
        description: "Publish to TestPyPI"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  get_version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get version from wavespec/__init__.py
        id: get_version
        run: |
          version=$(python3 - <<'PY'
          import re
          from pathlib import Path

          text = Path('wavespec/__init__.py').read_text(encoding='utf-8')
          m = re.search(r"__version__\s*=\s*['\"]([^'\"]+)['\"]", text)
          if not m:
              raise SystemExit('Could not find __version__ in wavespec/__init__.py')
          print(f"v{m.group(1)}")
          PY
          )
          echo "Found version: ${version}"
          echo "version=${version}" >> "$GITHUB_OUTPUT"

  tag:
    runs-on: ubuntu-latest
    needs: get_version
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

      - name: Check if tag exists
        run: |
          tag="${{ needs.get_version.outputs.version }}"
          if git rev-parse "$tag" >/dev/null 2>&1; then
            echo "Tag $tag already exists"
            exit 1
          fi

      - name: Create and push tag
        run: |
          tag="${{ needs.get_version.outputs.version }}"
          git tag "$tag"
          git push origin "$tag"

  build_artifacts:
    needs: [tag, get_version]
    uses: ./.github/workflows/ci.yml
    secrets: inherit

  release:
    runs-on: ubuntu-latest
    needs:
      - get_version
      - build_artifacts
    permissions:
      contents: write
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts
          merge-multiple: true

      - name: List artifacts
        run: |
          find release-artifacts -type f | sort

      - name: Create GitHub release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.get_version.outputs.version }}
          name: Release ${{ needs.get_version.outputs.version }}
          draft: false
          prerelease: false
          files: |
            release-artifacts/*

  publish_testpypi:
    runs-on: ubuntu-latest
    if: inputs.test
    needs:
      - build_artifacts
    steps:
      - name: Download distribution artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: List distributions
        run: |
          find dist -type f | sort

      - name: Publish package distributions to TestPyPI
        uses: pypa/gh-action-pypi-publish@v1.13.0
        with:
          user: __token__
          password: ${{ secrets.TEST_PYPI_API_TOKEN }}
          repository-url: https://test.pypi.org/legacy/

  publish_pypi:
    runs-on: ubuntu-latest
    if: ${{ !inputs.test }}
    needs:
      - build_artifacts
      - release
    steps:
      - name: Download distribution artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: List distributions
        run: |
          find dist -type f | sort

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@v1.13.0
        with:
          user: __token__
          password: ${{ secrets.PYPI_API_TOKEN }}
